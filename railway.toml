[build]
builder = "nixpacks"

[build.env]
# Force fresh installs to clear any stale cache
NIXPACKS_NO_CACHE = "1"

[deploy]
healthcheckPath = "/api/agent/tools"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
